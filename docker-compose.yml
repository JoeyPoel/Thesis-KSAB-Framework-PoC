version: '3.8'

services:
  talent-engine:
    build: .
    container_name: talent_engine_poc
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
    environment:
      - APP_ENV=production
    restart: always

networks:
  default:
    name: sap-concept-network
